{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ESNext", // 设置目标为最新的 ECMAScript 版本
    "lib": ["ESNext", "DOM"], // 包含 ESNext 和 DOM 的类型定义
    "module": "ESNext", // 使用 ESNext 模块系统

    /* Emit */
    "declaration": true, // 生成 .d.ts 声明文件
    "emitDeclarationOnly": true, // 仅生成声明文件，不生成 JavaScript 文件
    "outDir": "./dist", // 输出目录为 ./dist

    /* Interop Constraints */
    "esModuleInterop": true, // 支持 CommonJS 模块的默认导入
    "forceConsistentCasingInFileNames": true, // 强制文件名大小写一致

    /* Type Checking */
    "strict": true, // 启用所有严格类型检查选项
    "skipLibCheck": true, // 跳过库文件的类型检查

    /* Projects */
    "composite": true, // 启用项目引用支持
    "incremental": true // 启用增量编译
  },
  "include": ["src/**/*.ts"], // 包含 src 目录下的所有 .ts 文件
  "exclude": ["node_modules"] // 排除 node_modules 目录
}